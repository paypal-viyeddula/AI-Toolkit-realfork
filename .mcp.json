{
  "mcpServers": {
    "paypal-sandbox": {
      "type": "sse",
      "url": "https://mcp.sandbox.paypal.com/sse",
      "headers": {
        "Authorization": "Bearer ${PAYPAL_SANDBOX_ACCESS_TOKEN}"
      }
    }
  }
}
